# React Spectrum API Snapshots

Weekly rolling API surface diffs for react-spectrum, generated automatically.

## Structure
- `snapshots/` — weekly API surface JSON files
- `diffs/` — human-readable markdown diffs
- `latest.json` — current baseline (most recent snapshot)
